import 'package:flutter/material.dart';
import 'package:envelope/ui/components/tokens.dart';

const goldBevelShadow = <BoxShadow>[
  BoxShadow(
    color: VQColors.primary,
    blurRadius: 0,
    spreadRadius: 0,
    offset: Offset(0, 0),
  ),
  BoxShadow(
    color: VQColors.primary,
    blurRadius: 16,
    spreadRadius: 4,
    offset: Offset(0, 2),
  ),
];
